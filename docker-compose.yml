services:
  gsplayer:
    image: ${GSPLAYER_IMAGE}
    container_name: gsplayer
    restart: unless-stopped
    env_file: .env
    ports:
      - "${PORT:-3000}:3000"
    networks:
      - web

networks:
  web:
    external: true
